import web
import json

urls = ("/", "index")

class index:
    def GET(self):
        db = web.database(dbn='sqlite', db='insider_transactions.db')
        transactions = db.select('relevant_transactions')
        return json.dumps(transactions)

if __name__ == "__main__": 
    app = web.application(urls, globals())
    app.run() 

